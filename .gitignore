/target
recordings/
.DS_Store

# wasm-pack build output for the browser proof (build artifact, not source).
web-proof/pkg/

# Local scratch: Claude session memory and Supabase CLI cache.
memory/
supabase/.temp/
